% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_data_validation.R
\name{validate_cpue_input}
\alias{validate_cpue_input}
\title{Validate electrofishing input data}
\usage{
validate_cpue_input(catch_data, reach_metadata, strict = TRUE)
}
\arguments{
\item{catch_data}{A long-format catch data frame. Required columns:
\code{reach_id} (chr), \code{date} (Date), \code{pass_number} (int), \code{species}
(chr), \code{count} (int), \code{effort_seconds} (num). Optional columns:
\code{amperage} (num), \code{voltage} (num).}

\item{reach_metadata}{A reach metadata data frame. Required columns:
\code{reach_id} (chr), \code{length_m} (num). Optional columns:
\code{mean_width_m}, \code{area_m2}, \code{habitat_class}, \code{crew}, \code{gear}.}

\item{strict}{Logical. If \code{TRUE} (default), missing optional columns
trigger advisory warnings; if \code{FALSE}, missing optionals are silent.}
}
\value{
Invisibly returns \code{TRUE} if all validation checks pass.
Otherwise aborts with a classed error of class
\code{"cpue_validation_error"} (also \code{"triton_validation_error"}) whose
\code{failures} field contains the character vector of all detected
problems.
}
\description{
Runs the full battery of input validation checks against catch and
reach metadata tables. Every check runs regardless of earlier
failures, so the returned error lists every problem at once rather
than surfacing them one at a time.
}
\details{
Both tables must contain at least one row, and \code{reach_metadata} must contain
exactly one row per \code{reach_id}. Effort and sampled reach extents used as
denominators must be finite and positive; \code{area_m2} may be \code{NA} when it is
unavailable. Every catch row must have a non-missing, non-blank species
identifier.
}
\examples{
\dontrun{
validate_cpue_input(catch_data, reach_metadata)
}
}
\concept{validation}
