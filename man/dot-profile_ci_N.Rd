% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_population_estimation.R
\name{.profile_ci_N}
\alias{.profile_ci_N}
\title{Profile-likelihood confidence interval for removal abundance}
\usage{
.profile_ci_N(counts, level = 0.95, cap_mult = 50)
}
\arguments{
\item{counts}{Pass-ordered catch vector (length >= 2, \code{sum(counts) > 0}).}

\item{level}{Confidence level. Defaults to \code{0.95}.}

\item{cap_mult}{Upper search bound as a multiple of the total catch.}
}
\value{
A list with \code{lwr}, \code{upr} (integer abundance limits) and
\code{identifiable} (logical).
}
\description{
Inverts the constant-capture-probability removal likelihood to bracket
the abundance \code{N}. Unlike the symmetric large-sample Wald interval from
\code{N_se}, the profile limits respect the hard lower boundary \code{N >= T} (the
total catch -- a reach cannot hold fewer fish than were removed from it)
and the right-skew of the estimate, and they widen honestly when
depletion is weak.
}
\details{
The search runs \code{N} up to \code{cap_mult * T}, using integer binary searches
over the unimodal profile likelihood rather than allocating every
candidate abundance. Its memory use is therefore constant even for very
large catches. If the likelihood is still
admissible at that cap the upper limit is unbounded -- the data cannot
bound abundance from above, which happens at low capture probability --
and \code{identifiable} is returned \code{FALSE} so downstream summaries can flag
the series rather than report a falsely finite interval.
}
\keyword{internal}
