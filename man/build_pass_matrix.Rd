% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_analysis.R
\name{build_pass_matrix}
\alias{build_pass_matrix}
\title{Reshape long catch data into per-series pass-count vectors}
\usage{
build_pass_matrix(catch_data)
}
\arguments{
\item{catch_data}{A validated long-format catch data frame (see
\code{\link[=validate_cpue_input]{validate_cpue_input()}}). Must contain \code{reach_id}, \code{date},
\code{pass_number}, \code{species}, and \code{count}.}
}
\value{
A data frame with columns \code{reach_id}, \code{date}, \code{species},
\code{n_passes}, and a list-column \code{counts} holding the pass-ordered
catch vector for each series.
}
\description{
Collapses validated long-format catch data into one row per
\code{reach_id} x \code{date} x \code{species}, with an ordered numeric vector of
per-pass catches. Passes a species was not recorded on (but which were
conducted at that reach x date) are filled with zero, so the depletion
series correctly reflects a true zero catch rather than a dropped pass.
}
\examples{
catch <- data.frame(
  reach_id = "R1", date = as.Date("2025-06-01"),
  pass_number = c(1L, 2L, 3L), species = "BNT",
  count = c(45L, 18L, 7L), effort_seconds = 300
)
build_pass_matrix(catch)
}
\seealso{
Other analysis: 
\code{\link{analyze_cpue}()},
\code{\link{summarize_cpue}()}
}
\concept{analysis}
