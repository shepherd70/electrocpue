% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_population_estimation.R
\name{.carle_strub_ci_N}
\alias{.carle_strub_ci_N}
\title{Carle & Strub weighted-likelihood interval for removal abundance}
\usage{
.carle_strub_ci_N(
  counts,
  N_hat,
  alpha = 1,
  beta = 1,
  level = 0.95,
  cap_mult = 50
)
}
\arguments{
\item{counts}{Pass-ordered catch vector (length >= 2, \code{sum(counts) > 0}).}

\item{N_hat}{Carle & Strub integer point estimate.}

\item{alpha, beta}{Positive beta-prior parameters.}

\item{level}{Confidence/support level. Defaults to \code{0.95}.}

\item{cap_mult}{Search-cap multiplier applied to both total catch and the
point estimate. Including the point estimate is essential for strongly
informative custom priors that move \code{N_hat} well beyond total catch.}
}
\value{
A list with \code{lwr}, \code{upr} (abundance limits; \code{upr = Inf} when the
weighted likelihood remains admissible at the internal search cap) and
\code{identifiable} (logical).
}
\description{
Inverts the same beta-weighted removal likelihood whose adjacent-value
ratio is used by \code{\link[=carle_strub_estimate]{carle_strub_estimate()}} to locate its integer point
estimate. Keeping the prior parameters in the interval calculation avoids
reporting limits from a different model that may not contain the estimate.
}
\details{
The weighted likelihood, up to terms constant in \code{N}, is
\verb{N! / (N - T)! * B(T + alpha, k * N - X - T + beta)}. The likelihood-ratio
acceptance set is searched with constant-memory integer binary searches.
}
\keyword{internal}
