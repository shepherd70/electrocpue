% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_data_validation.R
\name{check_within_pass_consistency}
\alias{check_within_pass_consistency}
\title{Check effort and amperage are constant within each pass}
\usage{
check_within_pass_consistency(catch_data)
}
\arguments{
\item{catch_data}{See \code{\link[=validate_cpue_input]{validate_cpue_input()}}.}
}
\value{
Character vector of failure messages.
}
\description{
\code{effort_seconds} -- and \code{amperage} where present -- describe a pass, not
a species, so they are recorded identically on every species row of a
given \code{reach_id} x \code{date} x \code{pass_number}. When those rows disagree it
is a data-entry error: \code{\link[=analyze_cpue]{analyze_cpue()}} would otherwise resolve it
silently with \code{dplyr::first()}, keeping one value and discarding the
rest, so it is rejected here instead.
}
\keyword{internal}
