% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_population_estimation.R
\name{estimate_population}
\alias{estimate_population}
\title{Estimate population size from K-pass removal data}
\usage{
estimate_population(
  counts,
  method = c("auto", "zippin", "carle_strub"),
  alpha = 1,
  beta = 1,
  quiet = FALSE
)
}
\arguments{
\item{counts}{Numeric vector of catches per pass, ordered pass 1, 2,
..., k. Must be finite, non-negative integers with no \code{NA}.}

\item{method}{One of \code{"auto"} (default), \code{"zippin"}, or
\code{"carle_strub"}.}

\item{alpha, beta}{Prior parameters for the Carle & Strub estimator.
Each must be one finite positive number. Both default to \code{1} (uniform
prior), matching the original paper.}

\item{quiet}{Logical. If \code{TRUE}, suppress advisory warnings for
non-convergent or single-pass series. Useful when estimating many
series in a loop. Defaults to \code{FALSE}.}
}
\value{
A one-row data frame with columns: \code{method}, \code{n_passes},
\code{catch_total}, \code{N} (population estimate), \code{N_se}, \code{N_lwr}/\code{N_upr}
(profile-likelihood confidence limits for \code{N}; respect \code{N >= T} and
are asymmetric), \code{p} (per-pass capture probability), \code{p_se},
\code{converged} (logical), \code{identifiable} (logical; \code{FALSE} when the data
cannot bound \code{N} from above -- typically low capture probability),
and \code{note} (one of \code{"ok"}, \code{"single_pass"}, \code{"zero_catch"},
\code{"model_failure"}, \code{"no_convergence"}, \code{"assumption_violated"}).
}
\description{
Estimates abundance for a single removal series (one reach x date x
species) from successive-pass catch counts. Three methods are
available: the Zippin maximum-likelihood estimator, the Carle & Strub
weighted-likelihood estimator, and an \code{"auto"} mode that uses Zippin
when its model is valid and falls back to Carle & Strub otherwise.
}
\details{
Point estimates and variances follow the standard removal formulae
(Zippin 1956, 1958; Carle & Strub 1978) as implemented in the FSA
package, so results are directly comparable to \code{FSA::removal()}. A
zero total catch is reported as \code{N = 0} (no fish detected -> zero
density) rather than as a model failure, which is the more useful
convention for downstream CPUE work.

The removal model assumes catch declines across passes as the local
population is depleted, so the first pass should catch the most fish.
When some later pass catches as many as the first, or more, the
depletion assumption is violated: an estimator may still return a
numeric estimate, but it is not trustworthy. Such a series is flagged
with \code{note = "assumption_violated"} (and a warning) rather than \code{"ok"}.
The check spans every pass and applies to all methods, so \code{"auto"}
flags a non-depleting series whether Zippin converged on it or the
Carle & Strub fallback was used. (A non-depleting series so extreme that
the search does not converge is instead returned as
\code{note = "no_convergence"}, \code{N = NA}.)
}
\examples{
estimate_population(c(45, 18, 7))
estimate_population(c(38, 26, 12), method = "carle_strub")
}
\references{
Zippin, C. (1956). An evaluation of the removal method of estimating
animal populations. Biometrics 12:163-189.

Zippin, C. (1958). The removal method of population estimation.
Journal of Wildlife Management 22:82-90.

Carle, F.L. & Strub, M.R. (1978). A new method for estimating
population size from removal data. Biometrics 34:621-630.
}
\seealso{
Other estimation: 
\code{\link{carle_strub_estimate}()},
\code{\link{zippin_estimate}()}
}
\concept{estimation}
