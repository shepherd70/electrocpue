% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_analysis.R
\name{analyze_cpue}
\alias{analyze_cpue}
\title{Analyze electrofishing CPUE end to end}
\usage{
analyze_cpue(
  catch_data,
  reach_metadata,
  method = c("auto", "zippin", "carle_strub"),
  effort_basis = c("seconds", "amp_seconds"),
  alpha = 1,
  beta = 1,
  validate = TRUE
)
}
\arguments{
\item{catch_data}{A long-format catch data frame (see
\code{\link[=validate_cpue_input]{validate_cpue_input()}}).}

\item{reach_metadata}{A reach metadata data frame with at least
\code{reach_id} and \code{length_m}; \code{area_m2} is used for areal density when
present.}

\item{method}{Removal estimator passed to \code{\link[=estimate_population]{estimate_population()}}:
\code{"auto"} (default), \code{"zippin"}, or \code{"carle_strub"}.}

\item{effort_basis}{Denominator for the effort-standardized catch
rate: \code{"seconds"} (default) or \code{"amp_seconds"}. The latter requires
an \code{amperage} column in \code{catch_data} whose values are all present
(non-\code{NA}) and positive.}

\item{alpha, beta}{Carle & Strub prior parameters (see
\code{\link[=estimate_population]{estimate_population()}}).}

\item{validate}{Logical. If \code{TRUE} (default), run
\code{\link[=validate_cpue_input]{validate_cpue_input()}} first. Set \code{FALSE} only when inputs are
already known to be valid: validation also guards structural assumptions
the pipeline relies on -- notably pass numbers being contiguous from 1 --
and skipping it on, say, non-contiguous passes (1, 2, 4) lets them
collapse into a silently wrong depletion series.}
}
\value{
A data frame with one row per \code{reach_id} x \code{date} x \code{species}
and columns: keys (\code{reach_id}, \code{date}, \code{species}); estimation
(\code{method}, \code{n_passes}, \code{catch_total}, \code{N}, \code{N_se}, \code{p}, \code{p_se},
\code{converged}, \code{note}); reach extent (\code{length_m}, \code{area_m2}); effort
(\code{effort_seconds}, \code{effort_amp_seconds}, \code{effort_basis}, \code{cpue});
and density (\code{density_per_m}, \code{density_per_m2}). \code{area_m2}-dependent
columns are \code{NA} when areal metadata is absent.
}
\description{
Validates inputs, estimates removal-depletion abundance for every
\code{reach_id} x \code{date} x \code{species} series, standardizes effort, and
expresses abundance as spatial density. Returns one tidy row per
series, ready for summarizing or plotting.
}
\examples{
catch <- data.frame(
  reach_id = "R1", date = as.Date("2025-06-01"),
  pass_number = c(1L, 2L, 3L), species = "BNT",
  count = c(45L, 18L, 7L), effort_seconds = 300
)
meta <- data.frame(reach_id = "R1", length_m = 100)
analyze_cpue(catch, meta)
}
\seealso{
Other analysis: 
\code{\link{build_pass_matrix}()},
\code{\link{summarize_cpue}()}
}
\concept{analysis}
