% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_population_estimation.R
\name{carle_strub_estimate}
\alias{carle_strub_estimate}
\title{Carle & Strub K-pass removal estimator}
\usage{
carle_strub_estimate(counts, alpha = 1, beta = 1, quiet = FALSE)
}
\arguments{
\item{counts}{Numeric vector of catches per pass, ordered pass 1, 2,
..., k. Must be non-negative integers with no \code{NA}.}

\item{alpha, beta}{Prior parameters for the Carle & Strub estimator.
Both default to \code{1} (uniform prior), matching the original paper.}

\item{quiet}{Logical. If \code{TRUE}, suppress advisory warnings for
non-convergent or single-pass series. Useful when estimating many
series in a loop. Defaults to \code{FALSE}.}
}
\value{
A one-row data frame in the format described in
\code{\link[=estimate_population]{estimate_population()}}.
}
\description{
Weighted-likelihood (Bayesian, uniform-prior) depletion estimator
(Carle & Strub 1978). More robust than Zippin when depletion is weak,
and the recommended default for routine use.
}
\examples{
carle_strub_estimate(c(45, 18, 7))
}
\seealso{
Other estimation: 
\code{\link{estimate_population}()},
\code{\link{zippin_estimate}()}
}
\concept{estimation}
