% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_data_validation.R
\name{.positive_column_problem}
\alias{.positive_column_problem}
\title{Flag non-positive (and optionally NA) values in a numeric column}
\usage{
.positive_column_problem(values, prefix, requirement, na_ok = FALSE)
}
\arguments{
\item{values}{A vector; only acted on when numeric.}

\item{prefix}{Human-readable column reference for the message.}

\item{requirement}{Trailing clause stating the rule.}

\item{na_ok}{If \code{TRUE}, \code{NA} is permitted; if \code{FALSE} (default), \code{NA}
is treated as a failure.}
}
\value{
Character vector of failure messages (length 0 or 1).
}
\description{
Shared by the positivity checks (\code{\link[=check_effort_positive]{check_effort_positive()}} and
\code{\link[=check_reach_extent_positive]{check_reach_extent_positive()}}). A non-numeric column returns no
problem here: its wrong type is already reported by the kernel type
check, and comparing it would otherwise misbehave or abort the battery
mid-run (e.g. \code{factor <= 0} is all-\code{NA}, so a downstream
\verb{if (bad_n > 0)} would error with "missing value where TRUE/FALSE
needed" before \code{\link[=validate_cpue_input]{validate_cpue_input()}} could collate and abort once).
}
\keyword{internal}
