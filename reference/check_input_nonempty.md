# Check that an input table contains at least one row

Check that an input table contains at least one row

## Usage

``` r
check_input_nonempty(data, table_name)
```

## Arguments

- data:

  An input data frame.

- table_name:

  Human-readable argument name.

## Value

Character vector of failure messages.
