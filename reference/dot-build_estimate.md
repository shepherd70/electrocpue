# Assemble the standard one-row estimate result

Assemble the standard one-row estimate result

## Usage

``` r
.build_estimate(
  method,
  k,
  T,
  N,
  N_var,
  p,
  p_var,
  converged,
  note,
  N_lwr = NA_real_,
  N_upr = NA_real_,
  identifiable = FALSE
)
```
